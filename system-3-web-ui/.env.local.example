# ============================================================================
# SYSTEM 3 - Web UI Environment Configuration (macOS)
# ============================================================================

NEXT_PUBLIC_APP_NAME="Solana Trading Dashboard"
NEXT_PUBLIC_APP_VERSION="1.0.0"
NEXT_PUBLIC_PLATFORM="macOS"

NEXT_PUBLIC_TRADING_NODE_URL=http://localhost:3000
NEXT_PUBLIC_ENABLE_WEBSOCKET=true
NEXT_PUBLIC_REFRESH_INTERVAL_MS=5000

# Server-side only (optional): used by the dashboard API proxy
TRADING_NODE_API_KEY=

# UI protection: when set, UI requires this password via /unlock
UI_ACCESS_PASSWORD=
